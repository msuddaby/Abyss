# coturn configuration
# Copy to turnserver.conf and update values from your .env file:
#   cp turnserver.conf.example turnserver.conf
#
# Using REST API auth (ephemeral credentials):
#   use-auth-secret + static-auth-secret must match TURN_AUTH_SECRET in your .env

listening-port=3478
listening-ip=0.0.0.0
relay-ip=0.0.0.0
external-ip=YOUR_PUBLIC_IP
realm=abyss
use-auth-secret
static-auth-secret=CHANGE_ME_TO_A_STRONG_SECRET
lt-cred-mech
fingerprint
no-cli
verbose
