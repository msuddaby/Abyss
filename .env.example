# ===========================================
# Abyss - Central Configuration
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ===========================================

# ---- Database ----
POSTGRES_USER=abyss
POSTGRES_PASSWORD=changeme
POSTGRES_DB=abyss
POSTGRES_HOST=localhost
POSTGRES_PORT=5433

# ---- JWT ----
JWT_KEY=generate-a-random-64-char-string-here-for-hmac-sha512-signing!!!!
JWT_ISSUER=Abyss
JWT_AUDIENCE=Abyss

# ---- CORS (comma-separated) ----
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# ---- TURN / STUN ----
TURN_EXTERNAL_IP=YOUR_PUBLIC_IP
TURN_PORT=3478
TURN_USER=abyss
TURN_CREDENTIAL=changeme
TURN_REALM=abyss

# ---- Frontend (VITE_ prefix required by Vite) ----
VITE_API_URL=http://localhost:5000
VITE_STUN_URL=stun:stun.l.google.com:19302
VITE_TURN_URLS=turn:YOUR_PUBLIC_IP:3478,turn:YOUR_PUBLIC_IP:3478?transport=tcp
VITE_TURN_USERNAME=abyss
VITE_TURN_CREDENTIAL=changeme
